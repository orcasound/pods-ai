# Copyright (c) PODS-AI contributors
# SPDX-License-Identifier: MIT
---
name: Check CSV (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "check_csv workflow is disabled; see download_wavs.yml and bootstrap/README.md"
