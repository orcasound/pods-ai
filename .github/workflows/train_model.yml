# Copyright (c) PODS-AI contributors
# SPDX-License-Identifier: MIT
---
name: Train Model

on:
  workflow_dispatch:
    inputs:
      epochs:
        description: 'Number of training epochs'
        required: false
        default: '5'
        type: string
      batch_size:
        description: 'Training batch size'
        required: false
        default: '8'
        type: string
      output_dir:
        description: 'Output directory for the trained model'
        required: false
        default: 'model/multiclass'
        type: string
      hub_model_id:
        description: 'HuggingFace Hub model ID (e.g., username/model-name)'
        required: false
        default: 'davethaler/whale-call-detector'
        type: string
      push_to_hub:
        description: 'Push trained model to HuggingFace Hub'
        required: false
        default: true
        type: boolean

permissions:
  contents: read

jobs:
  train_model:
    runs-on: ubuntu-latest
    env:
      COSMOS_KEY: ${{ secrets.COSMOS_KEY }}
      HF_TOKEN: ${{ secrets.HF_TOKEN }}

    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@05e31511f85b41b11d1cf0ef85d0992719546e2c  # v2.21.0
        with:
          egress-policy: audit

      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          submodules: 'recursive'

      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: '3.14'

      - name: Install FFmpeg
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      - name: Download WAV files
        run: |
          python src/download_wavs.py

      - name: Train model
        run: >
          python src/train_podsai_model.py
          --output_dir ${{ inputs.output_dir }}
          --epochs ${{ inputs.epochs }}
          --batch_size ${{ inputs.batch_size }}
          ${{ inputs.push_to_hub && '--push_to_hub' || '' }}
          ${{ inputs.push_to_hub && format('--hub_model_id {0}', inputs.hub_model_id) || '' }}
